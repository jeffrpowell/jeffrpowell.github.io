import { ListRandomPipe } from './listRandom.pipe';

describe('ListRandomPipe', () => {
  it('create an instance', () => {
    const pipe = new ListRandomPipe();
    expect(pipe).toBeTruthy();
  });
});
